packeg afd;
class Main {
    main() {
        system.out.println("adfadf")
    }
}

class Person {
    name,
    email,
    mobile

    Person(name, email, mobile) {}
}

function Person(name, email, mobile) {
    this.name = name;
    this.email = email;
    this.mobile = mobile;
}